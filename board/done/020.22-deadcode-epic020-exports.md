# 020.22 — Deadcode cleanup for epic 020 exports

Description: After epic 020 local llama.cpp work, `npm run deadcode` fails on new unused exports. Prefer unexporting module-internal helpers; refresh `.tsprune-ignore` only for intentional public API / preload types / test hooks. Verify with local deadcode + `act` on `.github/workflows/deadcode.yml`.

## Acceptance criteria

- [x] Module-internal helpers unexported where safe (llamacppAssetsIpc handlers, downloadStateLabel, etc.)
- [x] `.tsprune-ignore` refreshed only for remaining intentional exports; diff reviewable
- [x] `npm run deadcode` passes
- [x] `act` on `.github/workflows/deadcode.yml` ends with Job succeeded
