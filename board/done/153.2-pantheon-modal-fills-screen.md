# 153.2 — Pantheon modal fills campaign content area

## Description

Opening **View pantheon** on the campaign page shows a dialog that stays stuck at the narrow generate-modal width (~520px) on the left, while the dimmed overlay already spans the content column. Match the world-history modal behavior: the pantheon dialog should fill the campaign content width (and use more vertical space for the deity list).

## Acceptance criteria

- [x] Pantheon modal CSS sets full content width under `campaign-review-overlay--content-width` (same pattern as world-history modal)
- [x] Pantheon body can use a larger viewport height than the old ~60vh/520px cap
- [x] Unit/source test asserts the fill-width and taller-body rules
- [x] `npm test` / `npm run lint` / `npm run build` / `npm run deadcode` pass
- [x] `act` pr-checks.yml + deadcode.yml succeed
