# 020.24 — Runtime backend GPU/CPU checkboxes

## Description

Replace the Local Settings runtime backend `<select>` with explicit **GPU (Vulkan)** and **CPU** checkboxes so either option is equally visible and choosable. Selection remains mutually exclusive and still drives acquire URL + persistence (`llamaCppRuntimeBackend`).

Follow-up to 020.23.

## Acceptance criteria

- [x] Settings Local section shows checkbox controls for GPU (Vulkan) and CPU (not a dropdown)
- [x] Checking one backend selects it (mutually exclusive) and persists via `onChange` / Settings
- [x] Unit tests cover checkbox presence and selection wiring
- [x] `npm test`, `npm run lint`, `npm run build`, `npm run deadcode`, and act CI pass
