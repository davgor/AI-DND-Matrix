# 020.32 — Merge split world JSON objects from local LLM

Local Qwen dumps show world gen returning **two** top-level JSON objects (`worldName`+`worldSummary`, then `worldHistory`) instead of one. `extractJsonObject` slices first `{`→last `}`, which is invalid JSON → `unparseable` for all three retries despite usable content.

## Acceptance criteria

- [x] `tryParseJson` merges consecutive brace-balanced JSON objects into one plain object (later keys win); unit-tested with the live Aeloria / Aetheris split dumps
- [x] Merged world payloads normalize + validate (reshape) for at least one captured live dump fixture
- [x] World prompt explicitly requires a single JSON object containing all three fields (no second object)
- [x] Campaign-create contract / generation tests still pass
- [x] `npm test` / `lint` / `build` / `deadcode` + act pr-checks + deadcode workflows pass
