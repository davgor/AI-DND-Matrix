# 020.21 — Fix oxlint/typecheck for epic 020 local llama work

Description: Split long functions/describes and extract helpers so oxlint (`max-lines-per-function`, `complexity`, unused imports) and `tsc -p tsconfig.node.json` pass for epic 020 Settings/llama.cpp files. Do not disable lint rules.

## Acceptance criteria

- [x] `npm run lint` passes with no epic-020-related max-lines/complexity/unused-import errors
- [x] `npx tsc --noEmit -p tsconfig.node.json` passes
- [x] Targeted vitest run for llamacpp + LlamaLocalSection + startupLlamaApply passes
- [x] No eslint-disable of max-lines/complexity rules
