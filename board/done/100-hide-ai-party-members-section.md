# 100 — Hide AI Party Members section on character setup

Remove the temporary "AI Party Members" heading/section from character setup UI (follow-up to 099). Party-member draft/submit wiring stays for later re-enable.

## Acceptance criteria

- [ ] Character setup no longer shows "AI Party Members" (or the party member section)
- [ ] Proceed / "Choose your race" still works with an empty party list
- [ ] `npm test`, `npm run lint`, `npm run build`, `npm run deadcode`, and act CI pass
