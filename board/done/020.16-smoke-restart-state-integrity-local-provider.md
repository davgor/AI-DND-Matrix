# Smoke parity: restart state integrity (local provider)

**Status: Dropped.** Epic 020 closed without a formal manual smoke pass. This checklist was never executed.

## Description
Mirror ticket 021.9 on the local-provider path: confirm closing and relaunching the app cleanly re-initializes the llama.cpp runtime and preserves all campaign/character/world state, even if the runtime is briefly unavailable at launch.

In attach mode, "re-initialize" means the app re-attaches/polls health — it does not require spawning a new `llama-server` if the user runs it externally.

## Prerequisites
- 020.7 gate smoke passed
- Reference model profile from `docs/research/llamacpp-local-runtime-2026-06-28.md`

## Acceptance Criteria
- [ ] With the local provider forced, after app restart the expected campaign/session/character/world state remains intact and playable
- [ ] The runtime process is re-initialized cleanly after restart with no orphaned prior process and no duplicate process spawn
- [ ] State integrity checks pass even when the local runtime is temporarily unavailable immediately after launch
