# 020.33 — Coerce religious faction for medium/heavy local dumps

Local Qwen faction dumps parse but fail `isValidGeneratedFactions` with `reason=invalid`: medium pressure + pantheon requires ≥1 `kind: "religious"`, yet the model emits civic/mercantile blocs (sometimes with `deityName`) and never sets `religious`. Spec already says religious is required after normalize when deities exist.

## Acceptance criteria

- [x] `normalizeGeneratedFactions(..., { deitiesPresent: true })` promotes one faith-linked (or first) faction to `religious` when pressure is medium/heavy and none exist — unit-tested with live Stormbringers dumps
- [x] `isValidGeneratedFactions` passes options through to normalize so validation matches create
- [x] `generateCampaignFactions` passes `deitiesPresent` into normalize
- [x] Create IPC surfaces a factions-specific generation message (like world)
- [x] Campaign-create checklist tests / full gate + act pass
