# 112.7 Tests and verification

## Description
Close the epic with end-to-end verification of metering correctness and the delivery gate.

## Acceptance Criteria
- [ ] Unit/integration tests cover: usage parse, purpose tagging on representative setup + play paths, persistence, aggregation, cost estimate, **and playtester export payload** (metadata present; secrets/prompts absent)
- [ ] Manual or scripted smoke: one campaign-create slice and one play NPC turn each leave usage rows with distinct purposes; export produces a file that re-aggregates to the same totals
- [ ] `npm test`, `npm run lint`, `npm run build`, `npm run deadcode` pass
- [ ] `act` runs `.github/workflows/pr-checks.yml` and `.github/workflows/deadcode.yml` successfully
- [ ] Epic **112** acceptance criteria / definition of done checked only after the above

