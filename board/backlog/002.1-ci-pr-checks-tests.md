# CI: PR check runs unit tests

## Description
Add the unit-test job to the PR-check GitHub Actions workflow.

## Acceptance Criteria
- [ ] `.github/workflows/pr-checks.yml` exists with a job that runs `npm test` on every PR targeting `main`
- [ ] Intentionally breaking a test and opening a PR shows the job failing; reverting shows it passing
