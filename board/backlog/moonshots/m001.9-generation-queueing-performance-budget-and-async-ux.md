# m001.9 Generation queueing/performance budget + async UX

## Description
Implement queueing and performance guardrails so image generation does not stall turn flow.

## Acceptance Criteria
- [ ] Background image jobs are queued and prioritized by gameplay relevance
- [ ] Performance budget defines max in-flight jobs and timeout behavior
- [ ] UI communicates async generation progress without blocking primary interactions
- [ ] Load-shedding/skipping strategy exists for per-turn updates under heavy load
