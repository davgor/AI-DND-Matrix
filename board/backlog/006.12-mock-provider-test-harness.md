# Mock provider test harness

## Description
Build a shared, scripted mock provider used across all agent tests so no automated test ever calls a real LLM.

## Acceptance Criteria
- [ ] A reusable mock provider supports scripting fixed responses (including malformed-schema responses) per test
- [ ] All other agent tickets' tests (006.1-006.9) use this shared harness rather than duplicating mock logic
