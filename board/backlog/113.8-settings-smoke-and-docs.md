# 113.8 Settings smoke + docs

## Description
Verify the expanded Settings flow in dev (and note packaged expectations), and update user-facing docs so GPT / Gemini / Grok setup is discoverable next to Claude / Player2.

## Acceptance Criteria
- [ ] Manual smoke checklist: pick each cloud provider, set key + model, Test connection, Save, run one generate path (or settings ping)
- [ ] Player2 and llama.cpp still reachable from the same dropdown
- [ ] README Setup section mentions the four cloud providers and that model is chosen in Settings
- [ ] Epic **113** definition of done checked only after verification gate (`npm test` / lint / build / deadcode / `act`)
