# Onboarding stage machine + defer play entry until guided flow completes

## Description
Extend `App.tsx` onboarding stages so mechanical character setup, guided identity interview, guided opening scene, and in-campaign play are distinct. Entering `PlayView` requires `guided_creation_phase === 'complete'` on the player character — not merely that a player character row exists.

## Acceptance Criteria
- [ ] `App.tsx` stage union adds `guidedIdentity` and `guidedOpeningScene` (or equivalent) between `characterSetup` and play
- [ ] `CharacterSetup` submit persists the player character and party but transitions to `guidedIdentity` instead of jumping straight to play
- [ ] `ReadyAppBody` does not render `PlayView` until guided creation is complete, even if a player character already exists
- [ ] Reloading the app mid-guided-creation resumes the correct phase from persisted `guided_creation_phase`
- [ ] Unit or integration test verifies play is blocked until phase is `complete`
