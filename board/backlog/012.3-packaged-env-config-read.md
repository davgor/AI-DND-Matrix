# Packaged app reads .env/provider config

## Description
Confirm the packaged app correctly reads `.env`/config for provider selection.

## Acceptance Criteria
- [ ] Packaging with Player2 configured in `.env` results in working agent calls in the packaged app
