# CI: PR check runs oxlint

## Description
Add the lint job to the PR-check workflow.

## Acceptance Criteria
- [ ] The PR-checks workflow runs `npm run lint` on every PR targeting `main`
- [ ] Intentionally introducing a lint violation and opening a PR shows the job failing; reverting shows it passing
