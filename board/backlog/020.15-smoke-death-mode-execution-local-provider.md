# Smoke parity: death mode execution (local provider)

## Description
Mirror ticket 021.8 on the local-provider path: confirm the campaign's configured death mode executes correctly via the engine, with llama.cpp contributing narration only.

## Acceptance Criteria
- [ ] With the local provider forced, death mode execution follows the configured rules and produces the expected continuity/outcome behavior
- [ ] Death-mode rule outcomes are computed by engine logic and only narrated by the local provider
- [ ] A failure in the local narration path does not bypass, alter, or invalidate death-mode rule outcomes
