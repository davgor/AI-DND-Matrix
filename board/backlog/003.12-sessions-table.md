# sessions table + repository

## Description
Create the `sessions` table (id, campaign_id, started_at, last_played_at) used by the sidebar ordering.

## Acceptance Criteria
- [ ] Migration creates the `sessions` table
- [ ] Repository functions: startSession(campaignId), touchLastPlayed(campaignId)
- [ ] Round-trip test: touching last_played_at updates the value and a listByLastPlayed query reflects the new order
