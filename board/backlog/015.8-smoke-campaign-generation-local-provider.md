# Smoke parity: campaign generation (local provider)

## Description
Mirror ticket 013.1 on the local-provider path: confirm campaign generation produces a playable starting scenario via llama.cpp, with no cloud-provider fallback and no corrupted save on failure.

## Acceptance Criteria
- [ ] With the local provider forced, generating a campaign from a free-text prompt produces a playable starting scenario with regions/NPCs/story thread visible in play
- [ ] No cloud-provider calls occur during the run
- [ ] If generation fails due to local runtime health, the flow exits gracefully with a typed error and no corrupted campaign save is left behind
