# EPIC: Main-screen settings + provider setup

Add a Settings entry from the main screen so players can configure AI providers without editing files manually: API-key providers, local llama setup/download path, and custom Player2 endpoint.

Broken down into sub-tickets 017.1-017.10. This epic is done when all of them are.

017.1 main-screen settings entry point · 017.2 settings shell + navigation · 017.3 provider mode selector + schema · 017.4 API key settings UI + secure save path · 017.5 llama local enable + runtime/model setup flow · 017.6 Player2 endpoint settings + connectivity test · 017.7 settings persistence + startup hydration · 017.8 validation/error UX + redaction rules · 017.9 runtime wiring from settings to provider selection · 017.10 settings smoke test (dev + packaged)
