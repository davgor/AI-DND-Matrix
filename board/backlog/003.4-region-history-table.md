# region_history table + repository + compression-candidate query

## Description
Create the `region_history` table (id, region_id, in_game_date, content, is_compressed) and the query that finds compression candidates for ticket 006.9.

## Acceptance Criteria
- [ ] Migration creates `region_history` with the listed columns
- [ ] Repository functions: create entry, listByRegion, markCompressed(id, newContent)
- [ ] A repository function returns entries for a region older than a given in-game-day threshold and not yet compressed
- [ ] Test seeds entries at various in_game_date values and confirms the threshold query returns exactly the expected subset
