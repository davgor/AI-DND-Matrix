# Settings persistence + startup hydration

## Description
Persist provider settings and hydrate them on startup so selected provider config is applied consistently.

## Acceptance Criteria
- [ ] Provider settings persist locally across app restarts
- [ ] Startup hydration loads persisted settings before provider selection is constructed
- [ ] Backward-compatible defaults exist for users with no saved settings yet
- [ ] Persistence path is tested for save/load/update flows
