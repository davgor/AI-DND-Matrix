# Initiative + turn structure

## Description
Implement combat initiative (rolled once per encounter, d20 + Agility) and the Action + Movement per-turn structure.

## Acceptance Criteria
- [ ] Initiative is rolled once per combatant at encounter start and the resulting order holds for the whole encounter, unit tested
- [ ] Turn structure enforces exactly one Action plus movement per turn; attempting a second Action in the same turn is rejected
