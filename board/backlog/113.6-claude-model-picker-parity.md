# 113.6 Claude model picker parity

## Description
Bring Claude settings in line with other cloud providers: model dropdown from the curated catalog (not only a hidden default / env default), plus optional custom model id.

## Acceptance Criteria
- [ ] Claude mode shows Model dropdown populated from the Claude catalog
- [ ] Custom model id path works the same as other cloud providers
- [ ] Existing saved `claudeModel` / env default migrates cleanly
- [ ] Test connection still works for Claude
- [ ] Unit/UI tests cover Claude model selection persistence
