/** Region backstory/history slice bundled with campaign detail for review and hub UI. */
export interface RegionExtras {
  regionId: string
  backstory: string
  recentHistory: string
  questHooks: string[]
}
