/** Packaged / installer icon (electron-builder Windows). */
export const APP_ICON_BUILD_ICO = 'build/icon.ico'

/** High-res PNG used for packaging conversion and window chrome. */
export const APP_ICON_BUILD_PNG = 'build/icon.png'

/** Filename placed in `process.resourcesPath` via electron-builder extraResources. */
export const APP_ICON_RESOURCE_NAME = 'icon.png'

/** In-app brand mark URL (vite public asset under `src/renderer/public`). */
export const APP_BRAND_MARK_SRC = '/app-icon.png'
